---
title: "fNIRS Parkinson navigation study - notebook"
bibliography: /Users/alexander.kvist/Zotero/zotero_library.bib
format:
  html:
    code-fold: true
    code-summary: "Show the code"
    embed-resources: true
    toc: true
    toc-depth: 3
    toc-expand: 3
    theme: flatly
  
editor: source
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
knitr::opts_chunk$set(fig.width=10, fig.height=5) 

# load library
library(jtools)
library(dplyr)
library(plyr, include.only = c("revalue"))
library(tidyr)
library(purrr)
library(stringr)
library(forcats)
library(arsenal)

library(ggplot2)
library(tidyverse)
library(patchwork)
library(cowplot)
library(labelled)

library(gt)
library(gtsummary)

```

# Introduction

This document is intended to be used as an "open lab notebook" for an exploratory, data-driven fNIRS study on navigated walking, analyzing an existing dataset, the Park-MOVE dataset collected between 2021 and 2023 [@franzen2023]. Note that while the data is in a central repository, access to the data is restricted and regulated by data transfer agreements.

The document is built from the project's GitHub repository where all code is also contained: <https://github.com/alkvi/fnirs_navigation_study>

How the dataset used in this study was prepared can be found in: <https://github.com/alkvi/fnirs_dataset_preparation>

## How has the data already been used?

We have a pilot study (<https://doi.org/10.1002/brb3.2948>) where we analysed the first 10 participants and looked at some condition effects. We have a validation study (<https://doi.org/10.1016/j.nicl.2024.103637>) where we looked at condition effects (single-task and dual—task walking) as well as a number of interaction effects. We have a hypothesis-driven study (<https://doi.org/10.1186/s12984-025-01864-w>) where we evaluated gait automaticity relationships to prefrontal activity during single-task and dual-task walking. This is the first study evaluating navigated walking in the full dataset. 

# Methods

The idea of the study is as follows:

- Use clustering on behavioral variables to find patterns of performance
- Evaluate demographics and clinical variables in and between clusters
- Evaluate neuroimaging data in clusters and contrast between clusters

## Hierachical clustering

Clustering was performed on performance data: gait variables, turning variables, task accuracy, task response time. Hierarchical clustering on distance matrices generated via unsupervised Random Forest was used. Imputation before clustering was also done. Number of clusters determined via majority voting through the NbClust package. Feature importance via supervised random forest on inferred cluster labels.

For details, see: (LINK)

## fNIRS analysis

System used is an 8x8 NIRSport2, with a montage over the prefrontal cortex. Sampling rate 10 Hz.

The fNIRS analysis is a fairly straightforward GLM analysis in the MATLAB NIRS AnalyzIR toolbox [@santosa2018].

Differential path-length factor (DPF) is set to depend on age [@scholkmann2013b]. HbO2 and HbR are combined by calculating the correlation-based signal improvement (CBSI) [@cui2010] signal. Channels are filtered based on a combined criteria of scalp coupling index (SCI) and peak spectral power (PSP) [@hernandez2020], with a bad channel detected as SCI < 0.7 or PSP < 0.1.

Subject level analysis is done with GLM + pre-whitening and autoregression (AR-IRLS) [@barker2013], with short-separation regressors.

Group level analyses is done with mixed effects models with subject as random intercept.

For details, see: (LINK)

# Results 

## Signal quality

The combined criteria on SCI and PSP marks a substantial amount of channels as bad.

From 20-30% of participants who had channels below criteria in the lower channels (around BA46), to up to 40% in the higher channels (around BA9).

![Channel-level signal quality in terms of % of participants in which channel was marked as below quality criteria](../figures_quality/bad_channels_combined_percent_07_01.JPG)

## Demographics and performance

```{r}

# Load behavioral data
source("../scripts/performance_data.R")
source("../scripts/subject_data.R")

performance_data <- load_subject_performance_data()
subject_data <- load_subject_data()

# Fix outlier in gait variables - two unreasonable values for step time var (>100)
idx <- which.max(performance_data$step_time_variability_protocol2_ST_walk)
performance_data$step_time_variability_protocol2_ST_walk[idx] <- NA
idx <- which.max(performance_data$step_time_variability_protocol2_ST_walk)
performance_data$step_time_variability_protocol2_ST_walk[idx] <- NA

# Also in navigation performance, two people who did not understand task
idx <- which.max(performance_data$acc_s3_wrong_st)
performance_data$acc_s3_wrong_st[idx] <- NA
idx <- which.max(performance_data$acc_s3_wrong_dt)
performance_data$acc_s3_wrong_dt[idx] <- NA
idx <- which.max(performance_data$acc_s3_wrong_tot)
performance_data$acc_s3_wrong_tot[idx] <- NA
idx <- which.max(performance_data$acc_s3_wrong_st)
performance_data$acc_s3_wrong_st[idx] <- NA
idx <- which.max(performance_data$acc_s3_wrong_dt)
performance_data$acc_s3_wrong_dt[idx] <- NA
idx <- which.max(performance_data$acc_s3_wrong_tot)
performance_data$acc_s3_wrong_tot[idx] <- NA

# Load cluster results
# Keep in mind cluster 1 = low performance for the OA group 
# but cluster 1 = high performance for the PD group
cluster_oa <- read.csv("../data/clusters_oa.csv") 
cluster_oa <- cluster_oa %>%
  mutate(cluster = case_when(
    cluster == 1 ~ "low performing",
    cluster == 2 ~ "high performing"
  ))

cluster_pd <- read.csv("../data/clusters_pd.csv") 
cluster_pd <- cluster_pd %>%
  mutate(cluster = case_when(
    cluster == 1 ~ "high performing",
    cluster == 2 ~ "low performing"
  ))

clusters <- bind_rows(cluster_oa, cluster_pd)

# Assign cluster function
performance_data <- performance_data %>% left_join(clusters %>% select(subject, cluster), by = "subject")
subject_data <- subject_data %>% left_join(clusters %>% select(subject, cluster), by = "subject")

# Separate out YA
performance_data <- performance_data %>% filter(group != "YA")
subject_data <- subject_data %>% filter(group != "YA")

performance_data$group <- droplevels(performance_data$group)
subject_data$group <- droplevels(subject_data$group)

# Separate data by groups to create tables per group
performance_data_oa <- performance_data %>% filter(group == "OA")
subject_data_oa <- subject_data %>% filter(group == "OA")
performance_data_pd <- performance_data %>% filter(group == "PD")
subject_data_pd <- subject_data %>% filter(group == "PD")

```

### Demographics

```{r}
#| warning: false

# Compare baseline demographics and performance.

tab1_demo <-
  tbl_summary(
    subject_data,
    include = c(sex, age, crf_utbildning_ar, weight, height, frandin_grimby.x, ramlat_12_man),
    by = group,
    missing = "no",
    type = list(
      frandin_grimby.x ~ "continuous",
      sex ~ "dichotomous"
    ),
    value = list(
      sex ~ "Female"
    ),
    digits = list(crf_utbildning_ar ~ 0, frandin_grimby.x ~ 0),
  ) |> 
  add_p() |>
  bold_p(t = 0.05) |>
  modify_header(
    label ~ "**Variable**",
  ) |>
  modify_caption("**Demographics**")

tab1_demo

tab1_clinical <-
  tbl_summary(
    subject_data,
    include = c(mb_total, g12_sum, tmt_2, tmt_4, tmt_4_tmt_2_contrast, cwit_3, ravlt_ret, vf_sum),
    by = group,
    missing = "no"
  ) |> 
  add_p() |>
  bold_p(t = 0.05) |>
  modify_header(
    label ~ "**Variable**",
  ) |>
  modify_caption("**Clinical & neuropsychological**")

tab1_clinical

```

### Performance 

```{r}
#| warning: false

# We want to compare the performance variables we use for clustering.
# Some values are missing, so calculate how many and append to table.
# These are the ones that are imputed in the clustering.

protocol2_vars <- c(
  "mean_walking_speed_protocol2_ST_walk",
  "mean_walking_speed_protocol2_ST_navigation",
  "mean_stride_length_protocol2_ST_walk",
  "mean_stride_length_protocol2_ST_navigation",
  "mean_cadence_protocol2_ST_walk",
  "mean_cadence_protocol2_ST_navigation",
  "step_time_variability_protocol2_ST_walk",
  "step_time_variability_protocol2_ST_navigation",
  "turns_velocity_protocol_2",
  "nav_s2_hesitation",
  "nav_s2_wrong"
)

tab1_performance <-
  tbl_summary(
    performance_data,
    include = all_of(protocol2_vars),
    by = group,
    missing = "no",
    type = list(c(nav_s2_hesitation, nav_s2_wrong) ~ "continuous")
  ) |>
  add_p() |>
  add_q() |>
  bold_p(t = 0.05, q = TRUE) |>
  add_n(
    statistic = "{p_miss}",
    col_label = "**Missing n (%)**"
  ) |>
  modify_caption("**Walking protocol performance, protocol 2**")

tab1_performance 

protocol3_vars <- c(
  "mean_walking_speed_protocol3_ST_navigation",
  "mean_walking_speed_protocol3_DT_navigation",
  "mean_stride_length_protocol3_ST_navigation",
  "mean_stride_length_protocol3_DT_navigation",
  "mean_cadence_protocol3_ST_navigation",
  "mean_cadence_protocol3_DT_navigation",
  "step_time_variability_protocol3_ST_navigation",
  "step_time_variability_protocol3_DT_navigation",
  "dt_cost_walk_speed_protocol3",
  "dt_cost_stride_length_protocol3",
  "dt_cost_step_time_variability_protocol3",
  "turns_velocity_protocol_3",
  "acc_s3_hesitation_st",
  "acc_s3_hesitation_dt",
  "acc_s3_wrong_st",
  "acc_s3_wrong_dt",
  "DT_protocol_3_stroop_acc",
  "DT_protocol_3_stroop_time"
)

tab2_performance <-
  tbl_summary(
    performance_data,
    include = all_of(protocol3_vars),
    by = group,
    missing = "no",
    type = list(c(acc_s3_hesitation_st,
                  acc_s3_hesitation_dt,
                  acc_s3_wrong_st,
                  acc_s3_wrong_dt) ~ "continuous")
  ) |>
  add_p() |>
  add_q() |>
  bold_p(t = 0.05, q = TRUE) |>
  add_n(
    statistic = "{p_miss}",
    col_label = "**Missing n (%)**"
  ) |>
  modify_caption("**Walking protocol performance, protocol 3**")

tab2_performance 
  
```

## Cluster demographics and performance

```{r}
#| warning: false

# Create tables
# This will warn about ties so we get approximate p values
tab1_oa <-
  tbl_summary(
    subject_data_oa,
    include = c(sex, age, crf_utbildning_ar, weight, height, frandin_grimby.x, ramlat_12_man),
    by = cluster,
    missing = "no",
    type = list(
      frandin_grimby.x ~ "continuous",
      sex ~ "dichotomous"
    ),
    value = list(
      sex ~ "Female"
    ),
    digits = list(crf_utbildning_ar ~ 0, frandin_grimby.x ~ 0)
  ) |> 
  add_p() |>
  bold_p(t = 0.05) |>
  modify_header(label = "**Variable**")

tab1_pd <-
  tbl_summary(
    subject_data_pd,
    include = c(sex, age, crf_utbildning_ar, weight, height, frandin_grimby.x, ramlat_12_man),
    by = cluster,
    missing = "no",
    type = list(
      frandin_grimby.x ~ "continuous",
      sex ~ "dichotomous"
    ),
    value = list(
      sex ~ "Female"
    ),
    digits = list(crf_utbildning_ar ~ 0, frandin_grimby.x ~ 0)
  ) |> 
  add_p() |>
  bold_p(t = 0.05) |>
  modify_header(label = "**Variable**")
  
tab1 <- tbl_merge(
  tbls = list(tab1_oa, tab1_pd),
  tab_spanner = c("**OA**", "**PD**")
) |>
  modify_caption("**Table 1. Demographics**")


tab2_oa <-
  tbl_summary(
    subject_data_oa,
    include = c(mb_total, g12_sum, tmt_2, tmt_4, tmt_4_tmt_2_contrast, cwit_3, ravlt_ret, vf_sum),
    by = cluster,
    missing = "no",
    type = list(c(mb_total, g12_sum) ~ "continuous"),
  ) |> 
  add_p() |>
  bold_p(t = 0.05) |>
  modify_header(label = "**Variable**")

tab2_pd <-
  tbl_summary(
    subject_data_pd,
    include = c(mb_total, g12_sum, tmt_2, tmt_4, tmt_4_tmt_2_contrast, cwit_3, ravlt_ret, vf_sum),
    by = cluster,
    missing = "no",
    type = list(c(mb_total, g12_sum) ~ "continuous"),
  ) |> 
  add_p() |>
  bold_p(t = 0.05) |>
  modify_header(label = "**Variable**")
  
tab2 <- tbl_merge(
  tbls = list(tab2_oa, tab2_pd),
  tab_spanner = c("**OA**", "**PD**")
) |>
  modify_caption("**Table 2. Clinical & neuropsychological**")

tab3_oa <-
  tbl_summary(
    performance_data_oa,
    include = all_of(protocol2_vars),
    by = cluster,
    missing = "no",
    type = list(c(nav_s2_hesitation, 
                  nav_s2_wrong) ~ "continuous"),
  ) |> 
  add_p() |>
  add_q() |>
  bold_p(t = 0.05, q=TRUE) |>
  modify_header(label = "**Variable**")

tab3_pd <-
  tbl_summary(
    performance_data_pd,
    include = all_of(protocol2_vars),
    by = cluster,
    missing = "no",
    type = list(c(nav_s2_hesitation, 
                  nav_s2_wrong) ~ "continuous"),
  ) |> 
  add_p() |>
  add_q() |>
  bold_p(t = 0.05, q=TRUE) |>
  modify_header(label = "**Variable**")
  
tab3 <- tbl_merge(
  tbls = list(tab3_oa, tab3_pd),
  tab_spanner = c("**OA**", "**PD**")
) |>
  modify_caption("**Table 3. Performance variables, protocol 2**")

tab4_oa <-
  tbl_summary(
    performance_data_oa,
    include = all_of(protocol3_vars),
    by = cluster,
    missing = "no",
    type = list(c(acc_s3_hesitation_st,
                  acc_s3_hesitation_dt,
                  acc_s3_wrong_st,
                  acc_s3_wrong_dt,
                  DT_protocol_3_stroop_acc, 
                  DT_protocol_3_stroop_time) ~ "continuous"),
  ) |> 
  add_p() |>
  add_q() |>
  bold_p(t = 0.05, q=TRUE) |>
  modify_header(label = "**Variable**")

tab4_pd <-
  tbl_summary(
    performance_data_pd,
    include = all_of(protocol3_vars),
    by = cluster,
    missing = "no",
    type = list(c(acc_s3_hesitation_st,
                  acc_s3_hesitation_dt,
                  acc_s3_wrong_st,
                  acc_s3_wrong_dt,
                  DT_protocol_3_stroop_acc, 
                  DT_protocol_3_stroop_time) ~ "continuous"),
  ) |> 
  add_p() |>
  add_q() |>
  bold_p(t = 0.05, q=TRUE) |>
  modify_header(label = "**Variable**")
  
tab4 <- tbl_merge(
  tbls = list(tab4_oa, tab4_pd),
  tab_spanner = c("**OA**", "**PD**")
) |>
  modify_caption("**Table 3. Performance variables, protocol 3**")


# Display the tables in the next block

```

### Demographics

```{r, results='asis'}

tab1
tab2

```

### Performance

::: {.column-page}

```{r, results='asis'}

tab3
tab4

```

:::

### Visualization

```{r}

# Plot helper function
make_domain_plot <- function(data, domain_name, disease_group,
                             show_y = TRUE, xlims = NULL) {
  
  plot_data <- data |>
    filter(
      domain == domain_name,
      group == disease_group
    )
  
  bg_data <- plot_data |>
    filter(!highlight)
  
  hl_data <- plot_data |>
    filter(highlight) |>
    mutate(
      xmin = pmin(low, high),
      xmax = pmax(low, high)
    )
  
  offset <- diff(xlims[[domain_name]]) * 0.03

  p <- ggplot(plot_data, aes(y = variable_label)) +
    scale_y_discrete(
      limits = levels(droplevels(plot_data$variable_label))
    ) +

    # faded background
    geom_segment(
      data = bg_data,
      aes(
        x = low,
        xend = high,
        yend = variable_label
      ),
      alpha = .2,
      colour = "grey60"
    ) +
    geom_point(
      data = bg_data,
      aes(x = low, colour = "low"),
      alpha = .2,
      size = 2
    ) +
    geom_point(
      data = bg_data,
      aes(x = high, colour = "high"),
      alpha = .2,
      size = 2
    ) +
  
    # highlighted
    geom_segment(
      data = hl_data,
      aes(
        x = low,
        xend = high,
        yend = variable_label
      ),
      linewidth = 0.8
    ) +
    geom_point(
      data = hl_data,
      aes(x = low, colour = "low"),
      size = 3
    ) +
    geom_point(
      data = hl_data,
      aes(x = high, colour = "high"),
      size = 3
    ) +
    scale_colour_manual(
      name = "Cluster",
      values = c(
        "low" = "#D55E00",
        "high" = "#0072B2"
      ),
      breaks = c("low", "high")
    ) +
  
    # labels only for significant variables
    geom_text(
      data = hl_data,
      aes(
        x = xmin - offset,
        label = round(xmin, 1)
      ),
      hjust = 1,
      size = 4
    ) +
    geom_text(
      data = hl_data,
      aes(
        x = xmax + offset,
        label = round(xmax, 1)
      ),
      hjust = 0,
      size = 4
    ) +
    labs(
      x = NULL,
      y = NULL
    ) +
    theme_bw(base_size = 14) +
    theme(
      axis.text.y = element_text(size = 13),
      axis.text.x = element_text(size = 13),
      legend.text = element_text(size = 13),
      legend.title = element_text(size = 14),
      panel.grid.major.y = element_line(colour = "grey90"),
      panel.grid.minor = element_blank(),
      legend.position = "none"
    )
  
  if (!is.null(xlims)) {
    p <- p +
      scale_x_continuous(
        limits = xlims[[domain_name]]
      )
  }

  if (!show_y) {
    p <- p +
      theme(
        axis.text.y = element_blank(),
        axis.ticks.y = element_blank()
      )
  }

  p
}

```

#### Protocol 2

```{r}

# Get labels
var_labels <- performance_data |>
  select(all_of(protocol2_vars)) |>
  var_label() |>
  unlist()

# Extract FDR-adjusted p from table
qvals <-
  tab3$table_body |>
  select(variable, q.value_1, q.value_2) |>
  pivot_longer(
    cols = starts_with("q.value"),
    names_to = "group",
    values_to = "q.value"
  ) |>
  mutate(
    group = recode(
      group,
      q.value_1 = "OA",
      q.value_2 = "PD"
    )
  )

# Set up data for dumbbell plot
plot_df <-
  performance_data |>
  pivot_longer(
    cols = all_of(protocol2_vars),
    names_to = "variable",
    values_to = "value"
  ) |>
  mutate(
    variable_label = var_labels[variable]
  ) |>
  group_by(group, cluster, variable, variable_label) |>
  summarise(
    median = median(value, na.rm = TRUE),
    .groups = "drop"
  ) |>
  left_join(
    qvals,
    by = c("group", "variable")
  ) |>
  mutate(
    highlight = q.value < 0.05
  )

# Make wide per cluster
plot_wide <-
  plot_df |>
  pivot_wider(
    names_from = cluster,
    values_from = median
  ) |>
  rename(
    high = `high performing`,
    low = `low performing`
  )

# Variable order in the plot
plot_wide <-
  plot_wide |>
  mutate(
    variable_label = factor(
      variable_label,
      levels = c(
        "Walking speed (straight), m/s",
        "Walking speed (navigation), m/s",
        "Stride length (straight), m",
        "Stride length (navigation), m",
        "Hesitations (navigation)",
        "Mistakes (navigation)",
        "Turn velocity (peak), deg/s",
        "Cadence (straight), steps/min",
        "Cadence (navigation), steps/min",
        "Step time variability (straight), ms",
        "Step time variability (navigation), ms"
      )
    )
  )

# Add domain (or rather panel to separate scales)
plot_wide <- plot_wide |>
  mutate(
    domain = case_when(
      variable %in% c(
        "mean_walking_speed_protocol2_ST_walk",
        "mean_walking_speed_protocol2_ST_navigation",
        "mean_stride_length_protocol2_ST_walk",
        "mean_stride_length_protocol2_ST_navigation",
        "nav_s2_hesitation",
        "nav_s2_wrong"
      ) ~ "Panel_1",

      variable %in% c(
        "turns_velocity_protocol_2",
        "mean_cadence_protocol2_ST_walk",
        "mean_cadence_protocol2_ST_navigation"
      ) ~ "Panel_2",

      variable %in% c(
        "step_time_variability_protocol2_ST_walk",
        "step_time_variability_protocol2_ST_navigation"
      ) ~ "Panel_3",

      TRUE ~ "Etc"
    ),
    
    domain = factor(
      domain,
      levels = c(
        "Panel_1",
        "Panel_2",
        "Panel_3"
      )
    )
  )

plot_wide <- plot_wide %>%
  mutate(variable_label = forcats::fct_rev(variable_label))

xlims <- list(
  "Panel_1" = c(0.0, 3.0),
  "Panel_2" = c(50, 160),
  "Panel_3" = c(0, 80)
)

panel_1 <-
  make_domain_plot(plot_wide, "Panel_1", "OA", show_y = TRUE, xlims=xlims) +
  make_domain_plot(plot_wide, "Panel_1", "PD", show_y = FALSE, xlims=xlims)

panel_2 <-
  make_domain_plot(plot_wide, "Panel_2", "OA", show_y = TRUE, xlims=xlims) +
  make_domain_plot(plot_wide, "Panel_2", "PD", show_y = FALSE, xlims=xlims)

panel_3 <-
  make_domain_plot(plot_wide, "Panel_3", "OA", show_y = TRUE, xlims=xlims) +
  make_domain_plot(plot_wide, "Panel_3", "PD", show_y = FALSE, xlims=xlims)

header_row <-
  patchwork::wrap_elements(
    grid::textGrob(
      "OA                                                    PD",
      gp = grid::gpar(fontsize = 12, fontface = "bold")
    )
  )

panel_1 <- panel_1 + theme(legend.position = "right")
panel_2 <- panel_2
panel_3 <- panel_3

final_plot <-
  (
    header_row /
    panel_1 /
    panel_2 /
    panel_3
  ) +
  plot_layout(
    guides = "collect",
    heights = c(0.15, 1.8, 1.2, 1)
  ) &
  theme(legend.position = "right")

final_plot

ggsave(
  "../figures/protocol_2_performance.svg",
  plot = final_plot,
  width = 10,
  height = 5,
  units = "in"
)

```

#### Protocol 3

```{r}

# Get labels
var_labels <- performance_data |>
  select(all_of(protocol3_vars)) |>
  var_label() |>
  unlist()

# Extract FDR-adjusted p from table
qvals <-
  tab4$table_body |>
  select(variable, q.value_1, q.value_2) |>
  pivot_longer(
    cols = starts_with("q.value"),
    names_to = "group",
    values_to = "q.value"
  ) |>
  mutate(
    group = recode(
      group,
      q.value_1 = "OA",
      q.value_2 = "PD"
    )
  )

# Set up data for dumbbell plot
plot_df <-
  performance_data |>
  pivot_longer(
    cols = all_of(protocol3_vars),
    names_to = "variable",
    values_to = "value"
  ) |>
  mutate(
    variable_label = var_labels[variable]
  ) |>
  group_by(group, cluster, variable, variable_label) |>
  summarise(
    median = median(value, na.rm = TRUE),
    .groups = "drop"
  ) |>
  left_join(
    qvals,
    by = c("group", "variable")
  ) |>
  mutate(
    highlight = q.value < 0.05
  )

# Make wide per cluster
plot_wide <-
  plot_df |>
  pivot_wider(
    names_from = cluster,
    values_from = median
  ) |>
  rename(
    high = `high performing`,
    low = `low performing`
  )

# Add domain (or rather panel to separate scales)
plot_wide <- plot_wide |>
  mutate(
    domain = case_when(
      variable %in% c(
        "mean_walking_speed_protocol3_ST_navigation",
        "mean_walking_speed_protocol3_DT_navigation",
        "mean_stride_length_protocol3_ST_navigation",
        "mean_stride_length_protocol3_DT_navigation",
        "acc_s3_hesitation_st",
        "acc_s3_hesitation_dt",
        "acc_s3_wrong_st",
        "acc_s3_wrong_dt",
        "DT_protocol_3_stroop_time"
      ) ~ "Panel_1",
      
      variable %in% c(
        "dt_cost_walk_speed_protocol3",
        "dt_cost_stride_length_protocol3",
        "dt_cost_step_time_variability_protocol3"
      ) ~ "Panel_2",

      variable %in% c(
        "turns_velocity_protocol_3",
        "DT_protocol_3_stroop_acc",
        "mean_cadence_protocol3_ST_navigation",
        "mean_cadence_protocol3_DT_navigation"
      ) ~ "Panel_3",

      variable %in% c(
        "step_time_variability_protocol3_ST_navigation",
        "step_time_variability_protocol3_DT_navigation"
      ) ~ "Panel_4",

      TRUE ~ "Etc"
    ),
    
    domain = factor(
      domain,
      levels = c(
        "Panel_1",
        "Panel_2",
        "Panel_3",
        "Panel_4"
      )
    )
  )

# Variable order in the plot
plot_wide <-
  plot_wide |>
  mutate(
    variable_label = factor(
      variable_label,
      levels = c(
        "Walking speed (ST), m/s",
        "Walking speed (DT), m/s",
        "Stride length (ST), m",
        "Stride length (DT), m",
        "Hesitations (ST)",
        "Hesitations (DT)",
        "Mistakes (ST)",
        "Mistakes (DT)",
        "DT cost walking speed, %",
        "DT cost stride length, %",
        "DT cost step time variability, %",
        "Turn velocity (peak), deg/s",
        "Accuracy (DT), %",
        "Cadence (ST), steps/min",
        "Cadence (DT), steps/min",
        "Step time variability (ST), ms",
        "Step time variability (DT), ms",
        "Answer time (DT), s" 
      )
    )
  )

plot_wide <- plot_wide %>%
  mutate(variable_label = forcats::fct_rev(variable_label))

xlims <- list(
  "Panel_1" = c(0.0, 3.0),
  "Panel_2" = c(0.0, 30.0),
  "Panel_3" = c(50, 160),
  "Panel_4" = c(0, 80)
)

panel_1 <-
  make_domain_plot(plot_wide, "Panel_1", "OA", show_y = TRUE, xlims=xlims) +
  make_domain_plot(plot_wide, "Panel_1", "PD", show_y = FALSE, xlims=xlims)

panel_2 <-
  make_domain_plot(plot_wide, "Panel_2", "OA", show_y = TRUE, xlims=xlims) +
  make_domain_plot(plot_wide, "Panel_2", "PD", show_y = FALSE, xlims=xlims)

panel_3 <-
  make_domain_plot(plot_wide, "Panel_3", "OA", show_y = TRUE, xlims=xlims) +
  make_domain_plot(plot_wide, "Panel_3", "PD", show_y = FALSE, xlims=xlims)

panel_4 <-
  make_domain_plot(plot_wide, "Panel_4", "OA", show_y = TRUE, xlims=xlims) +
  make_domain_plot(plot_wide, "Panel_4", "PD", show_y = FALSE, xlims=xlims)

header_row <-
  patchwork::wrap_elements(
    grid::textGrob(
      "OA                                                    PD",
      gp = grid::gpar(fontsize = 12, fontface = "bold")
    )
  )

panel_1 <- panel_1 + theme(legend.position = "right")
panel_2 <- panel_2
panel_3 <- panel_3
panel_4 <- panel_4

final_plot <-
  (
    header_row /
    panel_1 /
    panel_2 /
    panel_3 /
    panel_4
  ) +
  plot_layout(
    guides = "collect",
    heights = c(0.15, 3, 1, 1, 1)
  ) &
  theme(legend.position = "right")

final_plot

ggsave(
  "../figures/protocol_3_performance.svg",
  plot = final_plot,
  width = 10,
  height = 7,
  units = "in"
)

```


## Neuroimaging results

The high performing cluster in the OA group had an increase in activity in BA46 compared to rest during navigation in protocol 2. The low performing cluster did not have any significant increase of activity in BA46.

In the PD group, both clusters had an increase in dlPFC activity during navigation compared to rest in protocol 2, but only the high performing cluster had an increase in dlPFC activity during single- and dual-task navigation during protocol 3.

```{r, results='asis'}

format_results_table <- function(data) {
  data %>%
    gt() %>%
    fmt_number(
      columns = everything(),
      decimals = 2
    ) %>%
    fmt_number(
      columns = c(DF),
      decimals = 0
    ) %>%
    fmt(
      columns = c(p, FDR_p),
      fns = function(x) {
        ifelse(x < 0.001, "<.001", sprintf("%.3f", x))
      }
    ) %>%
    tab_style(
      style = cell_text(weight = "bold"),
      locations = cells_body(
        rows = FDR_p < 0.05
      )
    )
}

# Structure the results. Keep in mind cluster 1 = low performance
# for the OA group but cluster 1 = high performance for the PD group
# We will also FDR-adjust p values per protocol here
results_protocol_2 <- read.csv("../data/results_protocol_2.csv")
results_protocol_2 <- results_protocol_2 %>%
  filter(ROI == "BA46") %>%
  select(-AIC, -BIC, -formula, -type, -q) %>%
  rename(Condition = Contrast) %>%
  mutate(
    Condition = if_else(
      group == "PD",
      str_replace_all(
        Condition,
        c(
          "cond_" = "",
          "Navigated_walking" = "Navigation",
          "Straight_walking" = "Straight walking",
          ":cluster_1" = " (high performing)",
          ":cluster_2" = " (low performing)"
        )
      ),
      str_replace_all(
        Condition,
        c(
          "cond_" = "",
          "Navigated_walking" = "Navigation",
          "Straight_walking" = "Straight walking",
          ":cluster_1" = " (low performing)",
          ":cluster_2" = " (high performing)"
        )
      )
    ),
    FDR_p = p.adjust(p, method = "BH")
  ) %>%
  select(group, everything())


results_protocol_3 = read.csv("../data/results_protocol_3.csv")
results_protocol_3 <- results_protocol_3 %>%
  filter(ROI == "BA46") %>%
  select(-AIC, -BIC, -formula, -type, -q) %>%
  rename(Condition = Contrast) %>%
  mutate(
    Condition = if_else(
      group == "PD",
      str_replace_all(
        Condition,
        c(
          "cond_Navigation_and_Aud_Stroop" = "Navigation DT",
          "cond_Navigation" = "Navigation ST",
          ":cluster_1" = " (high performing)",
          ":cluster_2" = " (low performing)"
        )
      ),
      str_replace_all(
        Condition,
        c(
          "cond_Navigation_and_Aud_Stroop" = "Navigation DT",
          "cond_Navigation" = "Navigation ST",
          ":cluster_1" = " (low performing)",
          ":cluster_2" = " (high performing)"
        )
      )
    ),
    FDR_p = p.adjust(p, method = "BH")
  ) %>%
  select(group, everything())


```

### Protocol 2 ROI


```{r, results='asis'}

format_results_table(results_protocol_2)

```


### Protocol 2 OA

::: {layout-nrow=2 layout-ncol="3"}
![ST_walk: Low performing cluster](../figures/condition_contrast_figures/p2_oa_st_walk_cluster_1.svg)

![ST_walk: High performing cluster](../figures/condition_contrast_figures/p2_oa_st_walk_cluster_2.svg)

![ST_walk: High - Low](../figures/condition_contrast_figures/p2_oa_st_walk.svg)

![ST_nav: Low performing cluster](../figures/condition_contrast_figures/p2_oa_st_nav_cluster_1.svg)

![ST_nav: High performing cluster](../figures/condition_contrast_figures/p2_oa_st_nav_cluster_2.svg)

![ST_nav: High - Low](../figures/condition_contrast_figures/p2_oa_st_nav.svg)

:::

### Protocol 2 PD

::: {layout-nrow=2 layout-ncol="3"}
![ST_walk: Low performing cluster](../figures/condition_contrast_figures/p2_pd_st_walk_cluster_2.svg)

![ST_walk: High performing cluster](../figures/condition_contrast_figures/p2_pd_st_walk_cluster_1.svg)

![ST_walk: High - Low](../figures/condition_contrast_figures/p2_pd_st_walk.svg)

![ST_nav: Low performing cluster](../figures/condition_contrast_figures/p2_pd_st_nav_cluster_2.svg)

![ST_nav: High performing cluster](../figures/condition_contrast_figures/p2_pd_st_nav_cluster_1.svg)

![ST_nav: High - Low](../figures/condition_contrast_figures/p2_pd_st_nav.svg)

:::

### Protocol 3 ROI


```{r, results='asis'}

format_results_table(results_protocol_3)

```


### Protocol 3 OA

::: {layout-nrow=2 layout-ncol="3"}
![ST_nav: Low performing cluster](../figures/condition_contrast_figures/p3_oa_st_nav_cluster_1.svg)

![ST_nav: High performing cluster](../figures/condition_contrast_figures/p3_oa_st_nav_cluster_2.svg)

![ST_nav: High - Low](../figures/condition_contrast_figures/p3_oa_st_nav.svg)

![DT_nav: Low performing cluster](../figures/condition_contrast_figures/p3_oa_dt_nav_cluster_1.svg)

![DT_nav: High performing cluster](../figures/condition_contrast_figures/p3_oa_dt_nav_cluster_1.svg)

![DT_nav: High - Low](../figures/condition_contrast_figures/p3_oa_dt_nav.svg)

:::


### Protocol 3 PD

::: {layout-nrow=2 layout-ncol="3"}
![ST_nav: Low performing cluster](../figures/condition_contrast_figures/p3_pd_st_nav_cluster_2.svg)

![ST_nav: High performing cluster](../figures/condition_contrast_figures/p3_pd_st_nav_cluster_1.svg)

![ST_nav: High - Low](../figures/condition_contrast_figures/p3_pd_st_nav.svg)

![DT_nav: Low performing cluster](../figures/condition_contrast_figures/p3_pd_dt_nav_cluster_2.svg)

![DT_nav: High performing cluster](../figures/condition_contrast_figures/p3_pd_dt_nav_cluster_1.svg)

![DT_nav: High - Low](../figures/condition_contrast_figures/p3_pd_dt_nav.svg)

:::

# Disease severity

```{r}
#| warning: false


updrs_file <- paste("../../Park-MOVE_fnirs_dataset_v2/REDcap_data/UPDRS_data.csv", sep="")
measurement_date_file <- paste("../../Park-MOVE_fnirs_dataset_v2/measurement_dates.csv", sep="")

updrs_data <- read.csv(updrs_file)
updrs_data$updrs_3_total <- rowSums(updrs_data[, 35:66], na.rm = TRUE)
updrs_data <- updrs_data[c("id_nummer", "updrs_3_total")]
names(updrs_data)[names(updrs_data) == 'id_nummer'] <- 'subject'

# Get disease duration via diagnosis date and measurement date
diagnosis_data <- subject_data_pd[c("subject", "crf_pd_year_phone")]
measurement_dates <- read.csv(measurement_date_file, sep=";")
measurement_dates$measurement_date_t1 <- as.Date(measurement_dates$measurement_date_t1, format = "%Y-%m-%d")
measurement_dates$year <- format(measurement_dates$measurement_date_t1, "%Y")
measurement_dates <- measurement_dates[c("subject", "year")]
measurement_dates <- merge(diagnosis_data, measurement_dates, by = "subject", all = TRUE)
measurement_dates$disease_dur <- as.numeric(measurement_dates$year) - as.numeric(measurement_dates$crf_pd_year_phone)
measurement_dates <- measurement_dates[c("subject", "disease_dur")]

# Add measurment dates
subject_data_pd <- merge(subject_data_pd, measurement_dates, by = "subject", all = FALSE)
subject_data_pd <- merge(subject_data_pd, updrs_data, by = "subject", all = FALSE)

tab5_pd <-
  tbl_summary(
    subject_data_pd,
    include = c(
      led_total,
      updrs_3_total,
      disease_dur,
      ),
    by = cluster,
    missing = "no",
    type = list(c(disease_dur, 
                  updrs_3_total) ~ "continuous"),
  ) |> 
  add_p() |>
  bold_p(t = 0.05) |>
  modify_header(label = "**Variable**")

tab5_pd

```

# Manuscript

Finished manuscript will be linked here when ready.

# References
